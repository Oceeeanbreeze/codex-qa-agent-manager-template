# GitHub Repository Text Snippets

## Repository description
Public-safe QA multi-agent system template for Codex: routed roles, scoped memory, runtime manifest, doctor and health gates, full reconstruction docs, and battle-ready operator guidance.

## Short social preview
A docs-first, GitHub-safe template for reconstructing a quality-first QA agent system with routed roles, local-first memory, a parity-ready runtime manifest, battle-ready checklists, and secure setup guidance.

## Pinned README block
> This repository is the public bootstrap layer for a QA-focused multi-agent system in Codex.
> It reproduces the same system shape as the internal setup: role model, routing model, skill-loading logic, memory boundary shape, operator workflow, and security posture.
> It also documents the local runtime parameters that must be filled to reach parity on a real machine instead of stopping at a docs-only reconstruction.
> It is intentionally safe for GitHub publication and therefore does not include private vault contents, generated indexes, secrets, screenshots, logs, traces, or company-specific product knowledge.
> To reach battle-ready use on a real machine, the operator must still configure local runtime prerequisites, run doctor and health checks, and pass the battle-ready checklist.

## One-sentence pitch
Use this template to reconstruct a parity-ready QA agent system with safe defaults, explicit runtime parameters, runtime gates, and operator discipline, while keeping private memory and sensitive local state out of GitHub.
